/*
* 以下均是编译前配置，编译后会自动生成，编译后修改重启是无效的，需要重新编译。
*/



/**
 * 是否使用basePath
 * 注意需要和 next.config.ts 中的 basePath 保持一致
 * 生产环境不使用basePath，设置为空字符串
 */
export const basePath = '';


/**
 * 页面元数据
 */
export const appMetadata = {
    title: "好品，好生活",
    description: "好的品质，好的生活"
}


/**
 * 是否显示toast消息
 */
export const showToastMessages = false;

/**
 * 是否测试
 */
export const isTestApp = false;